# GeneratorCraft MkDocs Material Project

This project is configured to use MkDocs with the Material theme and is ready for GitHub Pages deployment. See `.github/workflows/` for an automatic deployment workflow.

